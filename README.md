# concurrency-patterns

A Go sandbox for practicing concurrency patterns — fan-in / fan-out, worker pool, pipeline, semaphore, futures, rate limiting, graceful shutdown, and more. Each pattern lives as a small, self-contained exercise focused on a single idiom.
